schema_version: '2.0'
title: 'Systemsbiology: General (COMBO_0001)'
description: Systemsbiology general comparative space with 2 models, configured as
  non-causal co-simulation.
models:
- repo: Biosimulant/models-systemsbiology
  alias: a_quantitative_1
  manifest_path: models/systemsbiology-sbml-a-quantitative-model-of-the-initiation-of-dna-re-model1812050001-model/model.yaml
  parameters:
    model_path: data/MODEL1812050001.xml
- repo: Biosimulant/models-systemsbiology
  alias: abbiati2021_avad_2
  manifest_path: models/systemsbiology-sbml-abbiati2021-avadomide-induced-neutropenia-model-model2412270001-model/model.yaml
  parameters:
    model_path: data/MODEL2412270001.xml
- repo: Biosimulant/models-observability
  alias: state_compare_mon
  manifest_path: models/observability-state-comparison-monitor/model.yaml
  parameters:
    max_points: 2000
    min_dt: 1.0
- repo: Biosimulant/models-observability
  alias: state_metrics_mon
  manifest_path: models/observability-state-metrics-monitor/model.yaml
  parameters:
    max_points: 2000
    min_dt: 1.0
runtime:
  duration: 60.0
  tick_dt: 0.01
  initial_inputs: {}
wiring:
- from: a_quantitative_1.state
  to:
  - state_compare_mon.state_a
  - state_metrics_mon.state_a
- from: abbiati2021_avad_2.state
  to:
  - state_compare_mon.state_b
  - state_metrics_mon.state_b
scientific_context:
  question: How do general mechanisms compare across these models?
  mode: comparative
  assumptions:
  - Model implementations are used as published in their curated manifests without
    biological reinterpretation.
  - Time integration uses a shared global tick compatible with model min_dt constraints.
  - Comparative (non-causal) mode is used because full deterministic IO coverage for
    causal coupling was not satisfied.
  expected_observables:
  - Model output trajectories under shared runtime settings.
  - Cross-model agreement/divergence in key state or metric signals.
  - Relative behavior comparison without causal linkage claims.
  limitations:
  - No new biology is introduced beyond what upstream models encode.
  - Cross-model semantic matching is rule-based and may under-connect uncertain routes.
  provenance:
  - 'systemsbiology-sbml-a-quantitative-model-of-the-initiation-of-dna-re-model1812050001-model
    :: biomodels_ebi:MODEL1812050001 :: https://www.ebi.ac.uk/biomodels/MODEL1812050001'
  - 'systemsbiology-sbml-abbiati2021-avadomide-induced-neutropenia-model-model2412270001-model
    :: biomodels_ebi:MODEL2412270001 :: https://www.ebi.ac.uk/biomodels/MODEL2412270001'
  visualization_rationale: A dedicated monitor model receives all participating model
    state streams (`state_a..state_d`) so trajectories can be compared in one place
    without claiming causal coupling when IO semantics are incomplete.
