schema_version: '2.0'
title: New Model
description: Toy model used to validate DynafluxR, as detailed in the publication.
  It can be used to explore systemsbiology new model2502210001 dynamics and compare
  simulation behavior across conditions.
standard: other
tags:
- systemsbiology
- sbml
- biomodels_ebi
- faithful
- curated
authors:
- Biosimulant Team
- Serguei Sokol
- Svetlana Dubiley
- Pauline Rouan
- Cyril Charlier
- Guy Lippens
upstream:
  id: biomodels_ebi:MODEL2502210001
  source_db: biomodels_ebi
  source_id: MODEL2502210001
  standard: sbml
  original_url: https://www.ebi.ac.uk/biomodels/MODEL2502210001
  download_url: https://www.ebi.ac.uk/biomodels/model/download/MODEL2502210001?filename=model.xml
  license: CC0
biosim:
  entrypoint: src.new_model_model2502210001_model:NewModelModel2502210001Model
  init_kwargs:
    model_path: data/MODEL2502210001.xml
  communication_step: 1.0
runtime:
  python_version: '3.12'
  dependencies:
    packages:
    - tellurium==2.2.11.2
io:
  inputs:
  - name: initial_model_state_a
    maps_to: A
    label: Initial Model State A
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `A`.
  - name: initial_model_state_k
    maps_to: K
    label: Initial Model State K
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `K`.
  - name: initial_model_state_j
    maps_to: J
    label: Initial Model State J
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `J`.
  - name: initial_model_state_i
    maps_to: I
    label: Initial Model State I
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `I`.
  - name: initial_model_state_h
    maps_to: H
    label: Initial Model State H
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `H`.
  - name: initial_model_state_g
    maps_to: G
    label: Initial Model State G
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `G`.
  outputs:
  - name: state
    description: Full SBML observable state record for reproducibility and downstream
      visualisation.
  - name: summary
    description: Largest-change and peak summary across simulated SBML observables.
  - name: species_labels
    description: Mapping from SBML observable symbols to display labels.
  - name: model_state_a
    maps_to: A
    label: A
    description: A. Maps to SBML symbol `A`.
  - name: model_state_k
    maps_to: K
    label: K
    description: K. Maps to SBML symbol `K`.
  - name: model_state_j
    maps_to: J
    label: J
    description: J. Maps to SBML symbol `J`.
  - name: model_state_i
    maps_to: I
    label: I
    description: I. Maps to SBML symbol `I`.
  - name: model_state_h
    maps_to: H
    label: H
    description: H. Maps to SBML symbol `H`.
  - name: model_state_g
    maps_to: G
    label: G
    description: G. Maps to SBML symbol `G`.
