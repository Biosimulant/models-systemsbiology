schema_version: '2.0'
title: Rohwer2001_Sucrose
description: SBML Level 2 code generated for the JWS Online project by Jacky Snoep
  using PySCeS . It can be used to explore systemsbiology rohwer2001 sucrose BioModels0000000023
  dynamics and compare simulation behavior across conditions.
standard: other
tags:
- generegulation
- systemsbiology
- sbml
- biomodels_ebi
- faithful
- curated
authors:
- Biosimulant Team
- J M Rohwer
- F C Botha
upstream:
  id: biomodels_ebi:BIOMD0000000023
  source_db: biomodels_ebi
  source_id: BIOMD0000000023
  standard: sbml
  original_url: https://www.ebi.ac.uk/biomodels/BIOMD0000000023
  download_url: https://www.ebi.ac.uk/biomodels/model/download/BIOMD0000000023?filename=BIOMD0000000023_url.xml
  license: CC0
biosim:
  entrypoint: src.rohwer2001_sucrose_biomd0000000023_model:Rohwer2001SucroseBiomd0000000023Model
  init_kwargs:
    model_path: data/BIOMD0000000023.xml
  communication_step: 1.0
runtime:
  python_version: '3.12'
  dependencies:
    packages:
    - tellurium==2.2.11.2
io:
  inputs:
  - name: initial_model_state_atp
    maps_to: ATP
    label: Initial Model State ATP
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `ATP`.
  - name: initial_model_state_adp
    maps_to: ADP
    label: Initial Model State ADP
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `ADP`.
  - name: initial_glycolysis
    maps_to: glycolysis
    label: Initial Glycolysis
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `glycolysis`.
  - name: initial_phos
    maps_to: phos
    label: Initial Phos
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `phos`.
  - name: initial_glcex
    maps_to: Glcex
    label: Initial Glcex
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `Glcex`.
  - name: initial_fruex
    maps_to: Fruex
    label: Initial Fruex
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `Fruex`.
  outputs:
  - name: state
    description: Full SBML observable state record for reproducibility and downstream
      visualisation.
  - name: summary
    description: Largest-change and peak summary across simulated SBML observables.
  - name: species_labels
    description: Mapping from SBML observable symbols to display labels.
  - name: atp
    maps_to: ATP
    label: ATP
    description: ATP. Maps to SBML symbol `ATP`.
  - name: adp
    maps_to: ADP
    label: ADP
    description: ADP. Maps to SBML symbol `ADP`.
  - name: glycolysis
    maps_to: glycolysis
    label: Glycolysis
    description: Glycolysis. Maps to SBML symbol `glycolysis`.
  - name: phos
    maps_to: phos
    label: Phos
    description: Phos. Maps to SBML symbol `phos`.
  - name: glcex
    maps_to: Glcex
    label: Glcex
    description: Glcex. Maps to SBML symbol `Glcex`.
  - name: fruex
    maps_to: Fruex
    label: Fruex
    description: Fruex. Maps to SBML symbol `Fruex`.
