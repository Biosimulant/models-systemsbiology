schema_version: '2.0'
title: Ueda2001_typeBtrichothecenes
description: 'This a model from the article: Critical study of and improvements in
  chromatographic methods for the analysisof type B trichothecenes. Mateo JJ, Llorens
  A, Mateo R, Jimenez M.'
standard: other
tags:
- systemsbiology
- sbml
- biomodels_ebi
- faithful
- curated
authors:
- Biosimulant Team
- H R Ueda
- M Hagiwara
- H Kitano
upstream:
  id: biomodels_ebi:MODEL1006230044
  source_db: biomodels_ebi
  source_id: MODEL1006230044
  standard: sbml
  original_url: https://www.ebi.ac.uk/biomodels/MODEL1006230044
  download_url: https://www.ebi.ac.uk/biomodels/model/download/MODEL1006230044?filename=MODEL1006230044_url.xml
  license: CC0
biosim:
  entrypoint: src.ueda2001_typebtrichothecenes_model1006230044_model:Ueda2001TypebtrichothecenesModel1006230044Model
  init_kwargs:
    model_path: data/MODEL1006230044.xml
  communication_step: 1.0
runtime:
  python_version: '3.12'
  dependencies:
    packages:
    - tellurium==2.2.11.2
io:
  inputs:
  - name: initial_tim_m
    maps_to: Tim_m
    label: Initial Tim M
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `Tim_m`.
  - name: initial_tim_c
    maps_to: Tim_c
    label: Initial Tim C
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `Tim_c`.
  - name: initial_per_m
    maps_to: Per_m
    label: Initial Per M
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `Per_m`.
  - name: initial_per_c
    maps_to: Per_c
    label: Initial Per C
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `Per_c`.
  - name: initial_pt_n
    maps_to: PT_n
    label: Initial Pt N
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `PT_n`.
  - name: initial_pt_c
    maps_to: PT_c
    label: Initial Pt C
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `PT_c`.
  outputs:
  - name: state
    description: Full SBML observable state record for reproducibility and downstream
      visualisation.
  - name: summary
    description: Largest-change and peak summary across simulated SBML observables.
  - name: species_labels
    description: Mapping from SBML observable symbols to display labels.
  - name: tim_m
    maps_to: Tim_m
    label: Tim M
    description: Tim M. Maps to SBML symbol `Tim_m`.
  - name: tim_c
    maps_to: Tim_c
    label: Tim C
    description: Tim C. Maps to SBML symbol `Tim_c`.
  - name: per_m
    maps_to: Per_m
    label: Per M
    description: Per M. Maps to SBML symbol `Per_m`.
  - name: per_c
    maps_to: Per_c
    label: Per C
    description: Per C. Maps to SBML symbol `Per_c`.
  - name: pt_n
    maps_to: PT_n
    label: PT N
    description: PT N. Maps to SBML symbol `PT_n`.
  - name: pt_c
    maps_to: PT_c
    label: PT C
    description: PT C. Maps to SBML symbol `PT_c`.
