schema_version: '2.0'
title: Maltsev2009_PacemakerCellModel_SteadyState
description: 'This a model from the article: Synergism of coupled subsarcolemmal Ca2+
  clocks and sarcolemmal voltage clocksconfers robust and flexible pacemaker function
  in a novel pacemaker cell model. It can be used to explore systemsbiology maltsev2009
  pacemakercellmodel steadystate model1006230104 dynamics and compare simulation behavior
  across conditions.'
standard: other
tags:
- systemsbiology
- sbml
- biomodels_ebi
- faithful
- curated
authors:
- Biosimulant Team
- Victor A Maltsev
- Edward G Lakatta
upstream:
  id: biomodels_ebi:MODEL1006230104
  source_db: biomodels_ebi
  source_id: MODEL1006230104
  standard: sbml
  original_url: https://www.ebi.ac.uk/biomodels/MODEL1006230104
  download_url: https://www.ebi.ac.uk/biomodels/model/download/MODEL1006230104?filename=MODEL1006230104_url.xml
  license: CC0
biosim:
  entrypoint: src.maltsev2009_pacemakercellmodel_steadystate_model1006230104_model:Maltsev2009PacemakercellmodelSteadystateModel1006230104Model
  init_kwargs:
    model_path: data/MODEL1006230104.xml
  communication_step: 1.0
runtime:
  python_version: '3.12'
  dependencies:
    packages:
    - tellurium==2.2.11.2
io:
  inputs:
  - name: initial_vm_vm
    maps_to: Vm_Vm
    label: Initial Vm Vm
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `Vm_Vm`.
  - name: initial_model_state_ri
    maps_to: RI
    label: Initial Model State Ri
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `RI`.
  - name: initial_r_j_sr_carel
    maps_to: R_j_SRCarel
    label: Initial R J Sr Carel
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `R_j_SRCarel`.
  - name: initial_model_state_qi
    maps_to: qi
    label: Initial Model State Qi
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `qi`.
  - name: initial_model_state_qa
    maps_to: qa
    label: Initial Model State Qa
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `qa`.
  - name: initial_model_state_pi
    maps_to: pi_
    label: Initial Model State Pi
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `pi_`.
  outputs:
  - name: state
    description: Full SBML observable state record for reproducibility and downstream
      visualisation.
  - name: summary
    description: Largest-change and peak summary across simulated SBML observables.
  - name: species_labels
    description: Mapping from SBML observable symbols to display labels.
  - name: vm_vm
    maps_to: Vm_Vm
    label: Vm Vm
    description: Vm Vm. Maps to SBML symbol `Vm_Vm`.
  - name: model_state_ri
    maps_to: RI
    label: RI
    description: RI. Maps to SBML symbol `RI`.
  - name: r_j_sr_carel
    maps_to: R_j_SRCarel
    label: R J SRCarel
    description: R J SRCarel. Maps to SBML symbol `R_j_SRCarel`.
  - name: model_state_qi
    maps_to: qi
    label: Qi
    description: Qi. Maps to SBML symbol `qi`.
  - name: model_state_qa
    maps_to: qa
    label: Qa
    description: Qa. Maps to SBML symbol `qa`.
  - name: model_state_pi
    maps_to: pi_
    label: Pi
    description: Pi. Maps to SBML symbol `pi_`.
