schema_version: '2.0'
title: Goldbeter1995_CircClock
description: To the extent possible under law, all copyright and related or neighbouring
  rights to this encoded model have been dedicated to the public domain worldwide.
  It can be used to explore systemsbiology goldbeter1995 circclock BioModels0000000016
  dynamics and compare simulation behavior across conditions.
standard: other
tags:
- systemsbiology
- sbml
- biomodels_ebi
- faithful
- curated
authors:
- Biosimulant Team
- A Goldbeter
upstream:
  id: biomodels_ebi:BIOMD0000000016
  source_db: biomodels_ebi
  source_id: BIOMD0000000016
  standard: sbml
  original_url: https://www.ebi.ac.uk/biomodels/BIOMD0000000016
  download_url: https://www.ebi.ac.uk/biomodels/model/download/BIOMD0000000016?filename=BIOMD0000000016_url.xml
  license: CC0
biosim:
  entrypoint: src.goldbeter1995_circclock_biomd0000000016_model:Goldbeter1995CircclockBiomd0000000016Model
  init_kwargs:
    model_path: data/BIOMD0000000016.xml
  communication_step: 1.0
runtime:
  python_version: '3.12'
  dependencies:
    packages:
    - tellurium==2.2.11.2
io:
  inputs:
  - name: initial_per_mrna
    maps_to: M
    label: Initial Per MRNA
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `M`.
  - name: initial_total_per
    maps_to: Pt
    label: Initial Total Per
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `Pt`.
  - name: initial_unphosphorylated_per
    maps_to: P0
    label: Initial Unphosphorylated Per
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `P0`.
  - name: initial_nuclear_per
    maps_to: Pn
    label: Initial Nuclear Per
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `Pn`.
  - name: initial_monophosphorylated_per
    maps_to: P1
    label: Initial Monophosphorylated Per
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `P1`.
  - name: initial_biphosphorylated_per
    maps_to: P2
    label: Initial Biphosphorylated Per
    description: Source state initial condition exposed as a model-specific control
      because no explicit intervention parameter is identifiable. Maps to SBML symbol
      `P2`.
  outputs:
  - name: state
    description: Full SBML observable state record for reproducibility and downstream
      visualisation.
  - name: summary
    description: Largest-change and peak summary across simulated SBML observables.
  - name: species_labels
    description: Mapping from SBML observable symbols to display labels.
  - name: per_mrna
    maps_to: M
    label: PER mRNA
    description: PER mRNA. Maps to SBML symbol `M`.
  - name: total_per
    maps_to: Pt
    label: total PER
    description: total PER. Maps to SBML symbol `Pt`.
  - name: unphosphorylated_per
    maps_to: P0
    label: unphosphorylated PER
    description: unphosphorylated PER. Maps to SBML symbol `P0`.
  - name: nuclear_per
    maps_to: Pn
    label: nuclear PER
    description: nuclear PER. Maps to SBML symbol `Pn`.
  - name: monophosphorylated_per
    maps_to: P1
    label: monophosphorylated PER
    description: monophosphorylated PER. Maps to SBML symbol `P1`.
  - name: biphosphorylated_per
    maps_to: P2
    label: biphosphorylated PER
    description: biphosphorylated PER. Maps to SBML symbol `P2`.
